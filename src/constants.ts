/**
 * @fileoverview Default values and constants for useSEO hook
 * @module use-seo/constants
 */

/**
 * Default Open Graph type for pages.
 */
export const DEFAULT_OG_TYPE = 'website';

/**
 * Default Twitter Card type.
 */
export const DEFAULT_TWITTER_CARD = 'summary_large_image';

/**
 * Default value for autoCanonical option.
 */
export const DEFAULT_AUTO_CANONICAL = true;

/**
 * Default value for preventDuplicates option.
 */
export const DEFAULT_PREVENT_DUPLICATES = true;

/**
 * Default value for validateUrls option.
 */
export const DEFAULT_VALIDATE_URLS = true;

/**
 * Recommended minimum title length.
 */
export const MIN_TITLE_LENGTH = 30;

/**
 * Recommended maximum title length.
 */
export const MAX_TITLE_LENGTH = 60;

/**
 * Recommended minimum description length.
 */
export const MIN_DESCRIPTION_LENGTH = 120;

/**
 * Recommended maximum description length.
 */
export const MAX_DESCRIPTION_LENGTH = 160;

/**
 * Recommended maximum number of keywords.
 */
export const MAX_KEYWORDS_COUNT = 10;
